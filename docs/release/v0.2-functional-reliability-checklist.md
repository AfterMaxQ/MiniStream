# MiniStream v0.2 functional reliability checklist

Complete this checklist on clean target builds and real target devices before
publishing a v0.2 release. Keep the evidence for each item with the release
record; an unchecked item is not accepted by a passing unit-test suite alone.

## Build and package

- [ ] Windows Release build completes with the unified Qt shell.
  Evidence: ______________________________________________
- [ ] `MiniStream-Setup.exe` contains the application, Qt/QML runtime, MSVC
      runtime, libsodium, and the bundled optional ViGEmBus installer.
  Evidence: ______________________________________________
- [ ] The installer offers the bundled ViGEmBus setup only when the service is
      absent and adds only a Private-profile MiniStream UDP firewall rule.
  Evidence: ______________________________________________
- [ ] Uninstall removes the MiniStream firewall rule and desktop shortcut.
  Evidence: ______________________________________________
- [ ] macOS Release build produces a launchable `MiniStream.app` and
      `MiniStream.dmg`.
  Evidence: ______________________________________________
- [ ] The final macOS bundle contains `NSLocalNetworkUsageDescription` and
      `NSScreenCaptureUsageDescription`, and resolves its libsodium dependency
      inside the bundle.
  Evidence: ______________________________________________

## Discovery and session lifecycle

- [ ] Allow control starts and stops discovery advertisement on Windows and
      macOS.
  Evidence: ______________________________________________
- [ ] Windows to macOS and macOS to Windows discovery works over Wi-Fi and
      Ethernet with the firewall enabled.
  Evidence: ______________________________________________
- [ ] Twenty consecutive Find devices runs show the actively broadcasting
      target without intermittent loss.
  Evidence: ______________________________________________
- [ ] macOS Local Network permission is exercised for first request, denial,
      and re-enable; each state has an understandable UI message.
  Evidence: ______________________________________________
- [ ] Discovery distinguishes searching, no devices, unusable interface,
      permission, bind, send, receive, and firewall-related failures.
  Evidence: ______________________________________________
- [ ] Hello/Accept, pairing-code confirmation, cancellation, disconnect, role
      switching, and window-close cleanup return both peers to a usable state.
  Evidence: ______________________________________________

## Media and input

- [ ] The baseline 1920x1080@60 H.264 SDR profile is negotiated by both peers
      and the capture, encoder, decoder, and scheduler use those dimensions.
  Evidence: ______________________________________________
- [ ] 1440p/4K HEVC are selected only when both advertised capability sets
      support them; HDR10 is selected only with an accepted HDR path.
  Evidence: ______________________________________________
- [ ] macOS controlled mode captures system/application audio at 48 kHz
      stereo and does not report the microphone as game audio.
  Evidence: ______________________________________________
- [ ] Keyboard, mouse, wheel, optional gamepad, and rumble work for a real
      session; input returns locally after disconnect or cancellation.
  Evidence: ______________________________________________
- [ ] Video packets received before CodecConfig are discarded, and unchanged
      codec parameters are not resent every frame.
  Evidence: ______________________________________________

## Network resilience and adaptation

- [ ] The receive loop drains a 5000-packet burst without a UI-timer packet
      ceiling.
  Evidence: ______________________________________________
- [ ] Configured FEC recovers loss within parity capacity, reports loss beyond
      parity as unrecoverable, and keeps encrypted datagrams within the MTU.
  Evidence: ______________________________________________
- [ ] Receiver feedback reaches the controlled peer; overload lowers the
      encoder and scheduler bitrate together, and stable recovery is bounded.
  Evidence: ______________________________________________
- [ ] Discovery, session, video, audio, input, FEC, and adaptation diagnostics
      expose packet/frame/queue/audio counters without logging keys or pairing
      secrets.
  Evidence: ______________________________________________

## Stability

- [ ] A 30–60 minute real-game session completes without crash or deadlock and
      without sustained memory, latency, audio-drift, or input degradation.
  Evidence: ______________________________________________

## Automated verification record

Record the exact build, test, UI-copy, package, and platform commands used for
this release here:

```text
Build:    ______________________________________________
Tests:    ______________________________________________
UI:       ______________________________________________
Package:  ______________________________________________
Platform: ______________________________________________
```
